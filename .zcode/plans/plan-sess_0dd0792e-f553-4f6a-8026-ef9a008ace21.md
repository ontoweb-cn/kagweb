# KAGWeb 派生实施方案（已含技术评审修正）

## 〇、技术评审结论（相对上一版的修正）

| # | 评审发现 | 修正措施 |
|---|---|---|
| R1 | `services/rag/` 中仅 `pipelines/pageindex/reasoning.py` 依赖 `runtime.agentic`（已核实，仅此一处） | Phase 1 删除 `runtime/agentic/` 时**顺带删除该文件**（本就注定删除），保证每个 Phase 结束时代码可导入 |
| R2 | **`learning/`（掌握度/Guided Learning）深编入会话层**：保留的 `api/routers/sessions.py:14`（LearningStore）、`services/session/_turn_runtime_shared.py`（LearningStore+topic_materials）、`services/session/turns/learning_adapter.py`、`services/courses_state.py`、`services/mastery_hints.py` 均引用（已核实） | Phase 1 新增子任务"session 层 learning/mastery 摘除"：删 learning_adapter.py、courses_state.py、mastery_hints.py，摘除 sessions.py 与 _turn_runtime_shared.py 中相关分支，然后整删 `kagweb/learning/` |
| R3 | 验证需要独立环境 | Phase 0/4：kagweb 内新建 `.venv` + `pip install -e ".[dev]"`；`web/` 内 `npm ci` |
| R4 | 合规 | 保留上游 LICENSE，新 README 注明 fork 自 KAGWeb (HKUDS) |
| R5 | redis 仅 `services/config/runtime_settings.py` config 级引用（已核实），cron 删除不影响 | redis 相关 env/配置保留，仅随全局改名 |
| R6 | contracts 生成依赖后端可导入 | contracts 重生成固定在 Phase 2 之后统一执行 |

其余结论沿用上一版调查（Agent Loop 双循环架构、RAG/KB 文件型存储无 SQL 表、DB 字段中性无需改库、~11.5k 处命名足迹、无独立 WIKI 功能）。

## 一、目标形态

- **chat = 框架壳**：stub `ChatCapability`（默认 `chat`），turn 返回 i18n 提示"尚未接入对话实现"并正常完成；未来 KAG 后端接入点 = `kagweb/runtime/orchestrator.py` → `kagweb/capabilities/chat/`。
- **保留**：LLM Provider 层（**chat_completions + responses 双协议完整保留**）、Orchestrator + Capability/Tool 注册表、StreamBus/流协议、会话存储（sqlite + PocketBase）、设置、多用户、Partners IM 通道（turn 同样进 stub）、Web 外壳、4 个自包含工具（brainstorm/web_search/paper_search/reason）。
- **删除**：`agents/` 全部、`runtime/agentic/`（迁出 usage.py）、10+ 个 loop 能力与扩展、RAG/KB 全链、embedding、Book/co_writer/notebook、learning/mastery、skills/eduhub、subagent、sandbox、cron、reading/video_learning、visualizers、courses。

## 二、实施步骤（项目地址 D:\workspace\kagweb，每 Phase 独立 commit）

### Phase 0 — 建项目
1. robocopy `D:\workspace\KAGWeb` → `D:\workspace\kagweb`（排除 `.git`、`.venv`、`node_modules`、`.next*`、`*.egg-info`、`__pycache__`、根 `data/`）。
2. `git init` + 初始 commit；建立 `.venv` 并 `pip install -e ".[dev]"`（基线验证）。

### Phase 1 — 删除 Agent Loop、卫星功能、session 层 learning 摘除
- 删 `kagweb/agents/`；迁出 `capability_result.py`→`capabilities/_shared.py`、`runtime/agentic/usage.py`→`services/llm/usage_tracker.py`；删 tool_composition.py（伙伴工具策略随工具收敛）。
- 删 `runtime/agentic/` 其余文件 + `services/rag/pipelines/pageindex/reasoning.py`（R1）；摘除 `services/llm/client.py:250`、`api/main.py:351`、`codebuddy_auth.py:107` 的池调用。
- 删 loop 能力与扩展目录；`registry.py` 只留 chat；新建 stub `capabilities/chat/`（+en/zh 文案）；`builtin_capabilities.py` 只注册 chat；`protocol.py` 删 LoopExtension 保留常量。
- **session 层 learning/mastery 摘除（R2）**：删 `turns/learning_adapter.py`、`services/courses_state.py`、`services/mastery_hints.py`；摘 `sessions.py`/`_turn_runtime_shared.py` 的 LearningStore/topic_materials 分支；删 `api/routers/mastery_path.py`；整删 `kagweb/learning/`（pyproject testpaths 同步）。
- 删卫星路由（question/notebook/agent_config/subagents/co_writer/books/skills/reading/video_learning 等，以 `api/main.py` 挂载清单为准）、`request_contracts.py` 相应引用；`context_builder.py` 摘要改单次 LLM 调用；CLI 删 skill 等命令。
- sandbox：删 `Dockerfile.runner`、设置段、相关 env。
- web：删 agents hub、books/co_writer/notebook/reading/courses/video/skills/research 页面、loop trace 组件、nav/locale keys；删对应 pytest 测试（tests/agents/**、tests/core/agentic/** 等）。

### Phase 2 — 删除 RAG/KB
- 整删：`knowledge/`、`services/rag/`（先迁 `file_routing.py` 扩展名表+decode_bytes → `utils/file_types.py` 并改 `document_extractor.py` 指向）、`services/embedding/`、`tools/rag_tool.py`、`api/routers/knowledge.py`、`services/config/knowledge_base_config.py`、`services/github_source/`、`services/web_source/`、`services/base_sync.py`、CLI `kb.py`。
- 手术：`core/context.py` 删 knowledge_bases 字段；`orchestrator.py:70-93` 删 pageindex 块；`tools/builtin` 缩为 4 工具；session turn payload kb 字段（容忍旧数据）；`multi_user/`（删 knowledge_access.py、grants/learning_access 相关 keys、paths）；`api/main.py`；settings/system embedding 段；`services/__init__.py`；`doctor._rag_check`；`partners/runtime._list_kb_names`；`pb_setup.py` 删 collection。
- 依赖：pyproject 删 pageindex/llama-index*/faiss-cpu/graphrag/rag-lightrag/rag-rerank、requirements/rag-rerank.txt，复核 numpy。
- web：删 features/knowledge/、knowledge-bases 页面、components/knowledge/、4 hooks、lib/knowledge-* 等；Composer 摘除 KnowledgeSelector；grants UI；**统一重生成 contracts（R6）**；locales 清理。

### Phase 3 — 全量改名 + 品牌清理
映射：`kagweb`→`kagweb`；`KAGWEB_*`→`KAGWEB_*`（45 env）；`KAGWebApp/Error/Parser`→`KagWeb*`；品牌文案→`KAGWeb`；生成类型随 contracts 消失。
- 目录：`kagweb/`→`kagweb/`、`kagweb_cli/`→`kagweb_cli/`、`kagweb_web/`→`kagweb_web/`、packaging、start 命令、k8s yaml。
- 配置：pyproject（name=kagweb、script `kagweb`、删 reading_extensions group）、.importlinter、ruff/mypy/pytest、web package.json（→kagweb-web）、locales 263 处、metadata、contracts 重生成（"KAGWeb API"）。
- 部署/CI：Dockerfile（用户/卷/uvicorn kagweb.api.main:app、镜像 `ghcr.io/YOUR_ORG/kagweb` 占位、NEXT_PUBLIC_BASE_PATH=/kagweb）、compose×4、K8s、workflows、pre-commit/.secrets 复查。
- 品牌：重写中英 README（注明 fork 自 HKUDS/KAGWeb，R4）；删 11 语言 README/past_releases/CITATION.cff/KAGWEB20；AGENTS.md 重写（**保留 Subpath 部署规则**）；User-Agent/Originator→"KAGWeb"；app_update 默认关闭。
- 词边界脚本替换 + 逐文件 diff 复核。

### Phase 4 — 验证（R3 环境）
1. 残留扫描：词边界 grep `kagweb|KAGWEB|\brag\b|RAGTool|kb_|knowledge_base|agent_loop|agentic|wiki`（白名单复核）。
2. 后端：`pip install -e ".[dev]"`、import、`kagweb --help`、pytest 全套、check_architecture.py。
3. 前端：`npm ci`、contracts:generate diff 干净、typecheck、build、剩余测试。
4. 冒烟：uvicorn（/docs 显示 KAGWeb API）、web dev（登录/设置/chat stub 提示）、`docker compose config`。
5. 交付 ARCHITECTURE.md：框架壳边界 + KAG 后端接入点（orchestrator→capability）。

## 三、规模与批次
Phase 1 ≈ 40%（含 R2 新增摘除）、Phase 2 ≈ 30%、Phase 3 ≈ 25%、Phase 4 ≈ 5%。按 Phase 分批执行、每批 commit + 冒烟后继续。