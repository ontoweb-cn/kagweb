# 批次四/五：子系统移除实施计划（合伙人+IM / 人格 / 学习者+监护人 / 连带清理）

## 范围与顺序（已确认的决策）

你此前已批准移除**合伙人+IM、人格、学习者+监护人**三者，因此原「决策 4 需产品决策」的学习者去留已解为**移除**。本计划把批次四/五拆成 **5 个独立提交（A–E）**，每阶段可独立回退、独立过 CI；硬约束内嵌在阶段顺序里。

**不在本轮**：MCP 重新定位（作 MCP 服务端暴露工具）、凭据打通（P3）、`require_learning_surface` 改名、能力目录（`presentation.tsx`）的产品化收缩——均记为后续。

---

## 阶段 A — `safe_filename` 迁移（解锁项，半小时级）

- 新建 `kagweb/utils/filenames.py`，迁入 `safe_filename`（实现原样：`kagweb/partners/helpers.py:36-39`）。
- 改 `kagweb/services/storage/attachment_store.py:38` 导入。
- 测试迁移：`tests/services/partners/test_safe_filename.py` → `tests/utils/test_safe_filename.py`（导入指向新位置）。其余 helpers 测试随阶段 B 删除。
- 验证：`pytest tests/services/storage tests/utils -q` + 全量。

## 阶段 B — 合伙人 + IM 通道移除（最大单体，~4.5 万行）

**后端删除**：`kagweb/partners/`、`kagweb/services/partners/`、`kagweb/services/partner_groups/`、`api/routers/{partners,partner_groups,_partners_channel_schema}.py`、`multi_user/partner_access.py`、`kagweb_cli/partner.py`（含 `main.py` 的 add_typer）。

**接线摘除**：
- `api/main.py`：`_start_partners`/`_stop_partners`/`_handle_background_command` 删除；`BackgroundLeaderSupervisor` 改为 `start_callbacks=[]`、`stop_callbacks=[]`、`control_callback=None`，**保留 `recovery_callback=application_container.recover_once`**；partners/partner_groups 路由与 ws 挂载（`:456-468`）删除。
- 后台命令子协议收缩：`BackgroundCommandKind` 4 个值全删（含零提交者的 `CRON_RELOAD`）；`submit_background_command` 删除；`background_leader.py` 的 read/ack 控制循环在 `control_callback=None` 下的路径同步简化（实施时核对该类对 `control_callback=None` 的既有守卫）。
- 会话链路：`core/turn_request.py` `partner_group_references`；`request_preparer.py` 回退链（`:510-514`）；`executor.py` `:265,438,470`；`_turn_runtime_shared.py` `_partner_group_references` + snapshot 键；`app/service.py:47` 偏好持久化；`source_inventory.py` 的 partner/partner_group 引用块（`:55,117-146,281-298,401-414,479-488,568-664`）。
- 鉴权/授权：`multi_user/partner_access.py` 删除；`multi_user/paths.py:183-186` partner 分支；`grants.py` 的 `partners` 字段 + normalize 循环；`api/routers/settings.py:486-491` `is_partner_user_id` 拒绝分支；`outputs.py` `_resolve_partner_output` + `visible_partners`。
- `services/storage/attachment_store.py` 已在阶段 A 切走。

**前端删除**：`web/app/(workspace)/partners/**`（5 页）、`web/components/partners/**`（32 文件）、`web/lib/{partners-api,partner-groups-api,partner-session,partner-draft,partner-chat-draft}.ts`、`web/tests/*partner*.test.ts`（7 个）；共享文件摘除分支：`ChatMessageList.tsx`（PartnerDraftCard 渲染）、`capability-routes.ts`（`/partners` 前缀）、`proxy-policy.ts`（RETIRED_PAGE_PATHS 增加 `/partners`、`/space/personas` 便于旧链接回落——personas 在阶段 D 删，此处一并加）、`sidebar-layout.test.ts`（夹具中的 `/partners` 等失效 href 改为现存 href）、`reconnecting-websocket.test.ts`（PartnerComposer 加载）、`capability-access.test.ts`、`features/chat/messages/ChatMessageList.tsx`、`TracePresentation.tsx` group chip、`ContextBudgetChip.tsx` `partner_turn_policy` 色键、`lib/chat-export.ts`、`lib/attachment-limits.ts` 文案、`AttachmentsSettingsSection.tsx` 文案。

**打包/CI**：`pyproject.toml` 删 `partners`/`tutorbot`/`matrix`/`matrix-e2e` extras，`all` 改为 `[dev, math-animator, video-learning]`；删 `requirements/{partners,matrix,matrix-e2e}.txt`；`.github/workflows/tests.yml:263` 缓存键与 `:271` partners.txt 安装行删除；Dockerfile/compose 的 `KAGWEB_EXTRAS` 注释更新。`scripts/install_extras.py` extras 清单更新。

**契约**：路由删除后运行 `python scripts/export_frontend_contracts.py` + `npm run contracts:generate`，提交再生的 4 个契约文件（`/api/partners` 等 50 处路径随之消失）。

**文档**：`ARCHITECTURE.md:40,249,250,252,253,262`、`AGENTS.md:81-82,130`、`docs/backend-architecture.md`（§1 规模表、§2 漏斗、§6 路由表、§7 角色表）— 移除 partners/IM 行并更新行数。

**验证**：`pytest tests/ -q` 预期 **本地 10 个 partner 失败随测试删除而消失**（这是本阶段的可观察收益）；`npm run check`；契约 `--check` 通过；pre-commit。确认全仓 grep `kagweb.partners|services.partners|partner_groups` 零残留（`kagweb/utils/filenames.py` 除外）。

---

## 阶段 C — 学习者 + 监护人移除（解锁 `locked_persona` 耦合）

**后端删除**：`multi_user/{learner_profile,guardians,device_credentials}.py`；`multi_user.py` 监护人 CRUD（`:221-326`）、学习者限制/凭据重置（`:347-427`）、`learner_grant` 回退与 `learning_policy`/`partner_count` 残留；`auth.py` 设备登录/心跳/设备 CRUD（`:571-631, :903-981`）、`_require_local_learner`、学习者档案自助/管理路由（`:1007-1084`）、`POST /users` 的 learner 预设引导（`:1146-1168`）、`AuthStatusResponse.learning_policy` 死字段；`services/auth.py` 的 `dcid` JWT 字段、`validate_device_token`、`begin_device_session`。
- `AccountPreset` 去掉 `"learner"`：存量 learner 行在 `identity.py:76` 载入时静默降级为 `standard`（既有机制，无需迁移）；`set_preset` 无路由调用者，一并删除。
- `grants.py`：`learning_policy`、`LEARNING_*` 常量、`learner_grant`、`_normalize_learning_policy`、`validate_grant` 的 learning_policy 段全删（`agent_loop`/`agent_loop_cli`/`exec_enabled` 等保留）；`multi_user/audit.py` 的 guardian 日志函数如无其他消费者一并删。
- `executor.py:398-405` learner_profile 注入块 + `:505` metadata 键（零读者，已核实）。

**前端删除**：`LearnerProfileSettingsSection.tsx`、`GuardianSettingsSection.tsx`、`features/multi-user/components/{LearnerProfileEditor,GuardianRelationshipsEditor}.tsx`、`lib/{guardian-api,profile-api}.ts`、GrantEditor 的 learning_policy 分区与 `conservativeLearningPolicy`、`types.ts` 的 `learning_policy`/`LearningPolicy`、`lib/auth.ts` 的 `learning_policy`/`preset` learner 值、admin users 页的 Learner 徽章/编辑器/预设项、`settings-nav.ts` 两个 category、`settings-access.ts` 的 `showLearnerOnly`/`showGuardianOnly` 及其 `isSettingsCategoryVisible` 分支、`settings-continuous-navigation.test.ts` 的 section 顺序、`guardian-management.test.ts`、`admin-user-presets.test.ts` 的 learning 断言、`space/personas` 之外的 `/space` 项不动。

**文档**：`backend-architecture.md` §7 两行、§1/§6 表行措辞；`backend-llm-deployment.md` 决策 4/6 耦合注记更新。

**验证**：全量 pytest（`test_device_credentials`、`test_profile_router`、`test_partner_access` 等随删）；`npm run check`；负面对照：门禁测试保持全绿（`required_service` 逻辑不受本阶段影响）。

---

## 阶段 D — 人格子系统移除（~1,600 行 + UI）

**后端删除**：`services/persona/`、`api/routers/personas.py` + `main.py` 挂载（`:379, :448`）、`executor.py:406-414` persona 加载分支 + `:471` `persona=active_persona` + `:506` metadata、`core/turn_request.py:89` `persona` 字段、`request_preparer.py` persona 偏好持久化（`:123, :148-156, :349-353`）、`core/context.py` `persona_context` 字段与文档、`capabilities/chat/capability.py:643-646` 的 persona grounding 块。
**前端删除**：`space/personas/page.tsx`、`PersonasSection.tsx`、`personas-api.ts`、`skill-slug.ts` + 测试、孤儿 `PersonaPicker.tsx`、composer 的 `PersonaSelector`（`ChatComposer`/`ComposerInput`/`ChatWorkspace`/`ChatSpaceMenu`/`ChatMessageList` persona chip）、`buildStartTurnInput.ts`/`start-turn.ts` persona 字段、`ChatStateAdapter` personaSelection 全链路、`SpaceDashboard` persona 卡片、`SessionActivityPanel` 入口、`session-api.ts` preferences.persona、`session-activity.ts` persona 计数。
**契约**：再生成（turn-protocol 的 persona 属性与 `/api/personas` 路径消失）。
**打包**：删 `services/persona/presets/`（3 个 PERSONA.md）与 `kagweb/skills/builtin/`（5 个 SKILL.md，决策 3a）后，`pyproject.toml:268` 的 `"**/*.md"` 与 `:260-264` 注释删除（`kagweb_cli = ["**/*.md"]` 保留——`kagweb_cli/README.md` 仍在）。
**setup/init.py**：`seed_presets()` 调用删除。
**验证**：全量 + 浏览器抽查 composer（persona 选择器消失、无 JS 报错）；负面对照：`start_turn` 带 `persona` 字段 → 被 forbid 拒绝（钉死契约收缩）。

---

## 阶段 E — 工具包移除 + 学习期改名 + 死词条（批次五可执行部分）

**工具包**：删 `kagweb/tools/`（含 23×2 个 hint YAML、`question_bank.py`、孤儿 `web_fetch.py`）；`runtime/registry/tool_registry.py` 剥离 `load_builtins`/`builtin_specs`/`compose_prompt_text`/`TOOL_ALIASES`（注册表保留给 MCP 注册，`get_tool_registry` 不再自动装载内置）；`scoped_registry.py` 同样剥离 prompting；`core/tool_protocol.py` 的 prompt-hint 协议成员删除；`interface_settings.py` 的 `sanitize_enabled_tools`/`get_enabled_optional_tools`、`settings.py:114,431` 的 `enabled_optional_tools` 残留、`multi_user/tool_access.py`、`request_preparer.py:270-290` 的 tools back-fill/门禁（`payload["tools"]` 本就无消费者，P0-2 已证）全删；`metadata_i18n.py` 的 `imagegen`/`videogen` 及已删能力描述项；孤儿 quiz 客户端 helper `recordQuizResults`（`session-api.ts`）删除（后端 quiz 路由与 `notebook_entries` 表保留——数据面不动，记为后续）。
**改名**（学习期结束）：`nav-entries.ts` 标签 `"Learning Agent"`→`Chat`、`"Learning Space"`→`Space`、分组 `"Learning"`→`Workspace`、图标 `GraduationCap`→`Layers`；`SpaceDashboard`/`SpaceMain` 标题同步；locale：`zh` 的对应键改为 `聊天`/`空间`/`工作台`，tooltip 改为中性描述，并删除两份 locale 的 86 个 `guidedLearning.*` 死键与 7 个具名死串（zh）；`sidebar-layout.test.ts` 夹具 href 改为现存集合。**布局持久化按 href**，href 不变（`/chat`、`/space`）所以存量用户布局不受影响。

**验证**：全量 pytest + `npm run check`（含 contracts:check）+ pre-commit + 浏览器抽查 `/chat`、`/space`、`/settings`。

---

## 全局验证协议（每阶段）

1. `pytest tests/ -q`（阶段 B 后本地应为 **0 failed**——10 个 partner 环境失败随测试删除消失）
2. `cd web && npm run check`（typecheck + node + vitest + contracts:check + audit + 预算）
3. `.venv/bin/python -m pre_commit run --all-files`
4. 推送 Gitee + GitHub → `gh run watch` 13 job 全绿 → 进下一阶段

**提交策略**：A/B/C/D/E 各 1-2 个提交；B/C/D 每阶段内先删后接线条收，出现中间态破损时在同提交内收口（保证每个提交点 `pytest`+`check` 可过）。

## 风险与边界

| 风险 | 处置 |
|---|---|
| B 阶段删除面巨大（~4.5 万行、跨 5 层） | 按本清单执行 + 每阶段全仓 grep 收尾（`kagweb.partners` 等零残留）；任一阶段可独立 revert |
| 存量 learner 用户降级为 standard | `identity.py:76` 既有机制；commit message 写明 |
| 存量会话 preferences 残留 persona/partners 键 | 归一化与 snapshot 均按白名单挑键，残留无害（有测试路径覆盖） |
| 契约再生遗漏 | CI 的 `contracts:check` 会红，属可发现错误 |
| 前端 composer 移除 PersonaSelector / 导航改名 | 产品可见变化，已获批准（决策 6 + 决策 4）；文案可后续调 |

## 明确不做（本轮外）

MCP 重新定位（作 MCP 服务端暴露工具）、`require_learning_surface` 改名、能力目录（`presentation.tsx`）产品化收缩、凭据打通（P3）、`notebook_entries` 表与 quiz 后端路由的数据面处理、`source_manifest` 字段去留（现仍被 capability 消费）。
