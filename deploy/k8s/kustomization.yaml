apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - kagweb.yaml
# Pin images for production instead of floating on :latest, e.g.:
# images:
#   - name: ghcr.io/hkuds/kagweb
#     newTag: 1.2.3
